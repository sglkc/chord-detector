\documentclass[pdflatex,sn-mathphys-num,iicol]{sn-jnl}

\usepackage{bookmark}
\usepackage{tabularx}
\usepackage{diagbox}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc, fit, backgrounds}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{amsthm}
\usepackage{mathrsfs}
\usepackage[title]{appendix}
\usepackage{xcolor}
\usepackage{textcomp}
\usepackage{manyfoot}
\usepackage{booktabs}
\usepackage{algorithm}
\usepackage{algorithmicx}
\usepackage{algpseudocode}
\usepackage{listings}

\theoremstyle{thmstyleone}
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}[theorem]{Proposition}
\theoremstyle{thmstyletwo}
\newtheorem{example}{Example}
\newtheorem{remark}{Remark}
\theoremstyle{thmstylethree}
\newtheorem{definition}{Definition}

\raggedbottom

\begin{document}

\title[Evaluating CNN vs. SVM for Piano Chord Recognition Under Acoustic Domain Shift]{Evaluating CNN vs. SVM for Piano Chord Recognition Under Acoustic Domain Shift}

\author*[1]{\fnm{Cikal} \sur{Gemintang Seya}}\email{cikalgemintangseya1@gmail.com}

\author[1]{\fnm{Jasman} \sur{Pardede}}\email{jasman@itenas.ac.id}

\affil*[1]{\orgdiv{Informatics}, \orgname{National Institute of Technology}, \orgaddress{\street{Neglasari}, \city{Bandung}, \postcode{40124}, \state{West Java}, \country{Indonesia}}}

\abstract{The generalization behavior of spectrogram-based audio classifiers under acoustic domain shift remains insufficiently characterized. This paper evaluates Constant-Q Transform (CQT) spectrogram features paired with two structurally distinct classifiers, a Convolutional Neural Network (CNN) and a hyperparameter-tuned Radial Basis Function Support Vector Machine (RBF-SVM), on a controlled 36-class piano chord recognition problem. Both models are trained on 7,200 audio samples recorded from a Techno T-9890i keyboard and subsequently evaluated under cross-domain conditions on a separate, independent 1,440-sample dataset from a Yamaha PSR-S910 keyboard in a different physical environment, establishing a rigorous cross-domain evaluation. Under in-domain 5-fold cross-validation, both classifiers achieve perfect accuracy and F1-scores of 1.00, confirming that CQT feature space is fully discriminative under stable conditions. However, a decisive divergence emerges under cross-domain evaluation, where the CNN retains strong generalizability with 0.89 accuracy and 0.87 macro F1-score, whereas the RBF-SVM completely collapses to 0.29 accuracy and 0.36 macro F1-score. Error analysis reveals that the CNN's residual errors are concentrated in harmonically proximate chord pairs, while the SVM exhibits a systematic single-class attractor collapse driven by the high sensitivity of global RBF kernel distances to global spectral envelope shifts. These results provide empirical evidence that the CNN's spatially local hierarchical feature learning is substantially more robust to acoustic covariate shifts than the global kernel boundaries of an RBF-SVM, offering critical structural insights for deploying spectrogram classifiers in real-world environments.}

\keywords{Constant-Q Transform, Convolutional Neural Network, Support Vector Machine, feature generalization, domain shift}

\maketitle

\section{Introduction}\label{sec1}

Spectrogram-based image classification has become a dominant paradigm across audio recognition tasks including environmental sound classification \cite{Wolf-Monheim2025}, speech recognition \cite{Zaman2023}, and music analysis \cite{Moysis2023}. In this paradigm a time-frequency representation is treated as a 2D image and processed by a convolutional classifier, exploiting the well-established strength of CNNs in learning spatially local hierarchical feature representations. A critical but often under-examined question is how robustly the feature-classifier system generalizes when the acoustic recording conditions at test time differ from those during training.

This mismatch, termed \emph{acoustic domain shift}, arises whenever instrument timbre, room acoustics, or recording device differ between the training corpus and the deployment environment \cite{Kusaka2024}\cite{Majchrzak2025}. Under such conditions a feature representation that is fully discriminative in-domain may lose discriminative power under cross-domain conditions, and a classifier whose decision boundary is optimally placed in the training feature space may become misaligned in the shifted feature space. Understanding which combinations of spectral features and classifier architectures are robust to such shifts is of substantial practical and theoretical importance.

The Constant-Q Transform (CQT) occupies a particular position among time-frequency representations, its logarithmically spaced frequency bins are aligned to the chromatic musical scale \cite{Li2021}\cite{Lacoste2007}, so harmonic overtone structures appear as translationally consistent patterns across the frequency axis regardless of instrument register. This structural alignment makes CQT spectrograms a natural testbed for investigating feature robustness, since the fundamental pattern of interest, the harmonic interval structure, is geometrically stable while secondary factors such as timbre and recording device frequency response introduce global and local distortions of that pattern \cite{Li2021}\cite{Telila2023}.

The piano chord classification problem provides a controlled vocabulary for such a study. With 36 classes formed by 12 chromatic root notes and 3 chord qualities (major, minor, diminished), the task requires fine-grained spectral discrimination: major and minor triads differ by a single semitone shift in the third, and diminished triads differ by a further semitone shift in the fifth, corresponding to shifts of one or two CQT frequency bins at the chosen resolution. This makes the task neither trivially easy nor artificially hard, appropriate for controlled generalization analysis.

The two classifiers evaluated here represent opposing strategies. CNN learns spatially localized hierarchical feature detectors whose convolutional kernels respond to small time-frequency neighborhoods \cite{OShea2015}\cite{Purwono2022}. RBF-SVM learns a maximum-margin hyperplane defined by kernel similarity to training support vectors, where the RBF kernel measures global Euclidean distance between flattened feature vectors \cite{Wainer2020}. Under acoustic domain shift these strategies are expected to diverge: CNN's locally defined representations should partially survive global spectral shifts, whereas SVM's globally defined kernel distances may be severely distorted by the same shifts \cite{Zaman2023}.

The existing literature reveals a critical systemic gap: contemporary music information retrieval (MIR) research heavily prioritizes expanding chord vocabulary size or introducing increasingly complex, parameterized architectures, such as conformer-based models \cite{Akram2025} or transformer-attention mechanisms \cite{Kusaka2024}. However, these systems are almost universally validated on static, in-domain benchmarks, leaving their structural generalizability unprobed. While a few recent studies have acknowledged the existence of an out-of-domain accuracy gap, they treat it purely as a data-scarcity issue to be bypassed via synthetic data augmentation \cite{Kusaka2024} or artificial generation strategies \cite{Majchrzak2025}, rather than diagnosing the underlying structural vulnerabilities of the chosen classifiers.

A controlled, systematic evaluation isolating how fundamentally distinct decision-boundary strategies (spatially local hierarchical feature learning versus global kernel methods) behave under an unaugmented acoustic domain shift remains entirely absent from the literature. This paper addresses this gap not by chasing minor accuracy improvements on over-fitted benchmarks, but by establishing a diagnostic baseline that maps the mathematical root causes of classifier resilience and collapse under real-world covariate shifts.

This paper addresses these gaps with a controlled design in which training and evaluation datasets are recorded on different keyboard instruments with different synthesis algorithms and recording devices in different physical environments, while all other parameters are held constant. The specific contributions are:

\begin{enumerate}[1.]

\item An experimental demonstration that CQT spectrograms are fully discriminative for 36-class piano chord classification under in-domain conditions, confirmed by perfect accuracy from both CNN and SVM across 5-fold cross-validation.

\item A cross-domain evaluation using two independently recorded keyboard datasets that differ in instrument, synthesis, device, and environment.

\item A comparative evaluation showing CNN and SVM are equivalent in-domain but diverge sharply under domain shift, with CNN achieving 0.89 accuracy and 0.87 macro F1-score versus SVM's 0.29 accuracy and 0.36 macro F1-score.

\item An error analysis showing CNN errors follow harmonic proximity patterns while SVM failure constitutes a global attractor collapse.

\item An empirical evidence that spatially local hierarchical feature learning is more robust to acoustic covariate shift than global kernel boundary learning, generalizing beyond the music domain.

\end{enumerate}

\section{Related work}\label{sec2}

The CNN-spectrogram pipeline is the dominant methodology for audio classification, with distribution shift identified as a key open challenge across speech, music, and environmental sound \cite{Zaman2023}\cite{Moysis2023}. Wolf-Monheim \cite{Wolf-Monheim2025} systematically compared spectral and rhythmic features including mel-scaled spectrograms, MFCCs, STFT chromagrams, CQT chromagrams, and CENS chromagrams for a deep CNN on the ESC-50 dataset, finding that feature choice substantially affects classification performance and that rankings may reverse under different evaluation conditions. This jointly motivates feature and classifier evaluation rather than either in isolation. Bartusiak and Delp \cite{Bartusiak2022} demonstrated that frequency-domain spectrogram CNN classifiers achieve high accuracy for subtle inter-distribution differences in audio signals, highlighting the discriminative capacity of spectral image representations even when distributions differ only in secondary acoustic characteristics.

Li \cite{Li2021} compared CQT, STFT, and CENS features as inputs to CNN and DNN classifiers for chord recognition, finding that CQT yielded the highest individual accuracy of 84.2\% compared to 75.3\% for STFT and 80.8\% for CENS, establishing CQT as the preferred feature for CNN-based chord classification. Telila et al.\ \cite{Telila2023} applied a CNN-CQT pipeline to polyphonic piano transcription and achieved frame-level accuracy of 88.2\%, confirming that CQT captures piano note frequency structure effectively. O'Hanlon and Sandler \cite{OHanlon2021} proposed FifthNet, a compact CNN architecture that exploits the musical structure embedded in CQT representations to achieve competitive chord recognition with substantially fewer parameters. Together these confirm that the structural alignment of CQT bins with musical intervals is a genuine inductive bias benefiting convolutional classifiers.

Majchrzak and Ma\'ndziuk \cite{Majchrzak2025} trained chord recognition models on artificially generated audio and evaluated on real recordings, demonstrating a persistent accuracy gap that deep feature learning partially but not fully bridges. Kusaka and Maezawa \cite{Kusaka2024} showed that data augmentation designed to simulate out-of-domain recordings improved note F1-score by 14.3 points, providing quantitative evidence that standard training without domain variation substantially underestimates cross-domain performance gaps.

SVM with PCP features has been applied successfully for in-domain chord and music classification \cite{Cui2022}\cite{Kamonsantiroj2024}, but neither study addressed cross-instrument or cross-environment generalization. The theoretical brittleness of RBF-SVM under feature distribution shift is well established \cite{Zaman2023}\cite{Wainer2020}, RBF kernel similarities between test samples and support vectors may change drastically when the marginal feature distribution shifts, causing systematic misclassification. CNNs, by contrast, learn spatially local representations that are partially invariant to global spectral translation, an architectural prior better aligned with the nature of cross-instrument acoustic variation \cite{OShea2015}. The present study provides a direct empirical test of this theoretical prediction.

\section{Methodology}\label{sec3}

The experimental pipeline is illustrated in Fig.~\ref{fig:methodology}. The study is structured around five phases: (1) dataset acquisition, (2) CQT feature extraction, (3) model training and hyperparameter optimization, (4) in-domain cross-validation, and (5) cross-domain evaluation. All experiments are implemented in Python 3.10 using TensorFlow 2.15, scikit-learn 1.7.2, Librosa 0.11.0, NumPy 1.26.4, Optuna 4.8.0, and Matplotlib 3.10.8. Training is conducted on an ASUS ROG Flow X13 (AMD Ryzen 7 6800HS, NVIDIA GeForce RTX 3050 Ti, 16\,GB RAM) with supplementary use of Google Colaboratory (T4/A100 GPU).

\begin{figure}[h]
  \centering

  \begin{tikzpicture}[
    node distance=0.1cm and 0.1cm,
    >={Stealth},
    base/.style={rectangle, draw, align=center, minimum height=0.6cm, font=\footnotesize, rounded corners=3pt, line width=0.5pt},
    main/.style={base, draw=blue!60!black, fill=blue!5, text width=6.0cm},
    cnn/.style={base, draw=teal!70!black, fill=teal!5, text width=2.85cm},
    svm/.style={base, draw=purple!70!black, fill=purple!5, text width=2.85cm},
    clear/.style={base, draw=green!60!black, fill=green!5, text width=6.0cm, font=\footnotesize},
    arrow/.style={->, line width=0.5pt, black!70}
  ]
    \node[draw, rounded corners=3pt, fill=black!7, text width=6.0cm, align=center,
    font=\footnotesize\bfseries, minimum height=0.45cm] (header) {Evaluating CNN vs. SVM for Piano Chord Recognition Under Acoustic Domain Shift};

    \node[main, below=0.3cm of header] (p1) {\textbf{1. Dataset acquisition}\\[2pt] 36 classes (12 roots $\times$ 3 qualities)\\ Train: 7{,}200 samples \& Eval: 1{,}440 samples\\ Two keyboards, two environments};

    \node[main, below=0.3cm of p1] (p2) {\textbf{2. CQT feature extraction}\\[2pt] 48\,kHz, 512 hop, 6 octaves, 36 bins/oct\\ Output: $216 \times 188 \times 1$ (dB spectrogram)};

    \node[cnn, below left=0.4cm and 0.15cm of p2.south, anchor=north east] (cnn1)
    {\textbf{3a. CNN path}\\[2pt] 4 Conv2D \& MaxPool\\ Filters: 64 $\to$ 256\\ Adam \& Dropout (0.5)};

    \node[cnn, below=0.25cm of cnn1] (cnn2) {\textbf{4a. 5-fold CV}\\[2pt] In-domain\\ Full retrain / fold};

    \node[svm, below right=0.4cm and 0.15cm of p2.south, anchor=north west] (svm1)
    {\textbf{3b. SVM path}\\[2pt] Flattened vectors\\ (40{,}608-dim.)\\ Optuna RBF tuning};

    \node[svm, below=0.25cm of svm1] (svm2) {\textbf{4b. 5-fold CV}\\[2pt] In-domain\\ Full retrain / fold};

    \node[main, below=0.45cm of $(cnn2.south)!0.5!(svm2.south)$, anchor=north] (p5)
    {\textbf{5. Evaluation and comparison}\\[2pt] In-domain test vs.\ cross-domain eval set\\ Generalization under acoustic domain shift};

    \node[clear, below=0.3cm of p5] (res)
    {\textbf{Comparative results}\\[2pt] Feature robustness, classifier generalization, domain-shift error analysis};

    % Upper paths
    \draw[arrow] (header) -- (p1);
    \draw[arrow] (p1) -- (p2);
    \draw[arrow] (p2.south) -- ++(0,-0.2) -| (cnn1.north);
    \draw[arrow] (p2.south) -- ++(0,-0.2) -| (svm1.north);
    \draw[arrow] (cnn1) -- (cnn2);
    \draw[arrow] (svm1) -- (svm2);

    % FIXED: Clean convergence into Box 5
    \coordinate (merge) at ($(p5.north)+(0,0.225)$);
    \draw[line width=0.5pt, black!70] (cnn2.south) |- (merge);
    \draw[line width=0.5pt, black!70] (svm2.south) |- (merge);
    \draw[arrow] (merge) -- (p5.north);

    % Lower path
    \draw[arrow] (p5) -- (res);
  \end{tikzpicture}

  \caption{Experimental pipeline for CQT feature robustness and classifier
  generalization evaluation. Both classifiers receive identical CQT features.
  The pipeline separates in-domain cross-validation from cross-domain
  evaluation to isolate the effect of acoustic domain shift.}
  \label{fig:methodology}
\end{figure}

\subsection{Dataset acquisition}\label{subsec3_1}

Audio recordings are collected using a custom web-based MIDI recording application that connects to a keyboard via USB MIDI, programmatically triggers chords by sending simultaneous MIDI note-on events, and captures the resulting audio through a connected microphone (Online Resource~1). This deterministic triggering approach eliminates manual playing errors while retaining organic acoustic properties arising from the instrument's physical resonance and synthesis.

The chord vocabulary covers 36 classes formed by the combination of 12 chromatic root notes (C, C$\sharp$, D, D$\sharp$, E, F, F$\sharp$, G, G$\sharp$, A, A$\sharp$, B) and 3 chord quality types (major, minor, diminished). All chords are voiced in the fourth octave, which provides optimal spectral coverage within the 6-octave CQT range and clear harmonic separability between qualities \cite{Telila2023} \cite{Harte2010}. The inclusion of the diminished quality tests classifier sensitivity to a harmonically dense chord type in which all intervals are minor thirds, creating greater inter-class harmonic proximity than major or minor chords \cite{Aidinis2024} \cite{Kantarelis2024}.

Two independent datasets are recorded to enable cross-domain evaluation:

\begin{enumerate}[1.]

\item \textbf{Training dataset}: 200 samples per class, 7,200 total. Recorded using a Xiaomi Redmi Pad 2 microphone from a Techno T-9890i keyboard with Acoustic Grand Piano voice. Note attack delays are randomized in the range 0--150\,ms to simulate natural playing articulation and prevent overfitting to perfectly synchronous note onsets.

\item \textbf{Evaluation dataset}: 40 samples per class, 1,440 total. Recorded using the internal microphone of an ASUS ROG Flow X13 laptop from a Yamaha PSR-S910 keyboard with Grand Piano voice, in a different physical room. Note attack delays are randomized in the range 0--100\,ms. Half of the samples per class are recorded at a relatively higher input volume level to introduce additional inter-sample acoustic variation.

\end{enumerate}

The two datasets differ systematically in instrument brand, piano voice synthesis, recording device, and room acoustics. No samples from the evaluation dataset were used at any stage of training, validation, or hyperparameter optimization. This strict separation ensures that cross-domain results measure genuine cross-domain generalization. All recordings are stored in OGG/OPUS format at 48,000\,Hz sample rate, 32-bit depth, mono channel, without post-processing filters. The distribution of audio durations across the training dataset is provided in Online Resource~2.

\subsection{CQT feature extraction}\label{subsec3_2}

Feature extraction uses the Constant-Q Transform via \texttt{librosa.cqt()}. The CQT computes a time-frequency representation in which each frequency bin $f_k$ satisfies a constant quality factor:
\begin{equation}
Q = \frac{f_k}{f_{k+1} - f_k} = \frac{1}{2^{1/B} - 1}
\label{eq:cqt}
\end{equation}
where $B$ is the number of bins per octave \cite{Li2021}\cite{Lacoste2007}. This logarithmic spacing aligns each bin with a musical semitone, making the spectrogram invariant to chord transposition and producing geometrically consistent harmonic patterns regardless of root pitch.

The extraction parameters are listed in Table~\ref{tab:cqt-extraction-params}. The configuration of 36 bins per octave (3 bins per semitone) provides frequency resolution sufficient to resolve the interval differences between major ($+4, +3$ semitones), minor ($+3, +4$ semitones), and diminished ($+3, +3$ semitones) triads \cite{Akram2025}\cite{OHanlon2021}. Each sample is truncated to a fixed duration of 2.0\,s, yielding a fixed frame length of $\lfloor2.0 \times 48000 / 512\rfloor = 188$ frames and a uniform feature shape of $216 \times 188 \times 1$.

\begin{table}[h]
\caption{CQT feature extraction parameters}\label{tab:cqt-extraction-params}
\begin{tabular}{@{}ll@{}}
\toprule
Parameter & Value \\
\midrule
Sample rate & 48{,}000\,Hz \\
Hop size & 512 samples \\
Octaves & 6 \\
Bins per octave & 36 \\
Total bins & 216 \\
Frequency range & C1 (32.70\,Hz) to C7 (2{,}093\,Hz) \\
Window & Hann \\
Frame length & 188 \\
Output shape & $216 \times 188 \times 1$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{CNN architecture and training}\label{subsec3_3}

The CNN architecture is adapted from Wolf-Monheim \cite{Wolf-Monheim2024}\cite{Wolf-Monheim2025}, originally designed for environmental sound classification on ESC-50 \cite{Purwono2022}. Its compact four-block convolutional structure without recurrent layers or attention mechanisms is appropriate for fixed-window classification from static spectrograms in this study.

Table~\ref{tab:cnn-architecture} specifies the complete layer sequence. Input batch normalization ensures consistent CQT magnitude statistics across samples and recording conditions, which is particularly important for cross-domain robustness since different recording environments may produce different absolute magnitude ranges. The four Conv2D-MaxPooling2D blocks build a progressive feature hierarchy: the first block (64 filters) captures low-level spectral texture; the second (128 filters) encodes mid-level harmonic interval patterns; the third and fourth (256 filters each) represent high-level chord quality abstractions. The $3\times3$ receptive field matches the 1--2 bin frequency shifts distinguishing chord types, while MaxPooling provides spatial subsampling and limited translation invariance \cite{OShea2015}\cite{Purwono2022}. Dropout (rate 0.5) prevents co-adaptation and serves as an implicit regularizer against overfitting to training instrument timbre.

\begin{table}[h]
\caption{CNN architecture specification}\label{tab:cnn-architecture}
\begin{tabularx}{\columnwidth}{@{}lXl@{}}
\toprule
Layer & Output shape & Notes \\
\midrule
BatchNorm & $216{\times}188{\times}1$ & Input normalization \\
Conv2D & $216{\times}188{\times}64$ & $3{\times}3$, ReLU, same \\
MaxPool & $108{\times}94{\times}64$ & $2{\times}2$, stride 2 \\
Conv2D & $108{\times}94{\times}128$ & $3{\times}3$, ReLU, same \\
MaxPool & $54{\times}47{\times}128$ & $2{\times}2$, stride 2 \\
Conv2D & $54{\times}47{\times}256$ & $3{\times}3$, ReLU, same \\
MaxPool & $27{\times}23{\times}256$ & $2{\times}2$, stride 2 \\
Conv2D & $27{\times}23{\times}256$ & $3{\times}3$, ReLU, same \\
MaxPool & $13{\times}11{\times}256$ & $2{\times}2$, stride 2 \\
Flatten & 36{,}608 & \\
Dense & 256 & ReLU \\
Dropout & 256 & rate 0.5 \\
Dense & 36 & Softmax \\
\bottomrule
\end{tabularx}
\end{table}

Training configuration follows Table~\ref{tab:cnn-training-config}. Stratified random sampling with seed 42 ensures balanced class representation and reproducibility. \texttt{ReduceLROnPlateau} reduces the learning rate by factor 0.1 when validation loss stagnates for 2 epochs; \texttt{EarlyStopping} terminates training after 6 epochs without validation loss improvement. The final model is evaluated on the 10\% reserved test set, excluded from all training and validation phases.

\begin{table}[h]
\caption{CNN training configuration}\label{tab:cnn-training-config}
\begin{tabular}{@{}ll@{}}
\toprule
Parameter & Value \\
\midrule
Random seed & 42 \\
Train / Val / Test split & 80\% / 10\% / 10\% \\
Batch size & 32 \\
Initial learning rate & 0.001 \\
Optimizer & Adam \\
Weight initialization & Glorot uniform \\
Loss & Categorical cross-entropy \\
Max epochs & 50 \\
EarlyStopping patience & 6 epochs \\
ReduceLROnPlateau & factor 0.1, patience 2 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{SVM hyperparameter optimization}\label{subsec3_4}

An RBF-kernel SVM is configured using scikit-learn's \texttt{SVC}. Each $216 \times 188$ CQT matrix is flattened to a 40,608-dimensional vector prior to classification. Hyperparameter optimization uses Optuna with a \texttt{RandomSampler} over 10 trials with the search space $C \in \{0.1, 1, 10, 100, 1000\}$ and $\gamma \in \{10^{-6}, 10^{-5}, 10^{-4}, 10^{-3}\}$ following Wainer and Fonseca \cite{Wainer2020}. Each trial is evaluated using stratified 5-fold cross-validation accuracy on the training dataset.

\subsection{5-fold cross-validation}\label{subsec3_5}

Stratified 5-fold cross-validation is performed on the full 7,200-sample training dataset to assess the statistical robustness of in-domain performance independently of a single train/test split. In each fold, 80\% is allocated to training and 20\% to testing. Both classifiers are fully retrained on each fold. For the CNN, each fold involves complete re-initialization and retraining from scratch with a fixed per-fold random seed. For the SVM, each fold likewise involves complete retraining of a new RBF-SVM using the fixed Optuna-selected hyperparameters, with the feature scaler refit exclusively on that fold's training partition to prevent leakage across folds.

\subsection{Evaluation metrics}\label{subsec3_6}

Classification performance is evaluated using accuracy, precision, recall, and F1-score derived from the confusion matrix \cite{Hossin2015}. For 36-class classification, macro-averaged metrics are reported, assigning equal weight to each class. Macro averaging is appropriate for generalization analysis as it penalizes class-level failure equally regardless of class frequency, in contrast to weighted averaging which inflates apparent performance when a small number of dominant classes are correctly classified.

\section{Results and discussion}\label{sec4}

\subsection{CQT feature extraction}\label{subsec4_1}

Feature extraction transforms all 8,640 audio recordings into $216 \times 188$ CQT decibel matrices. Fig.~\ref{fig:cqt-spectrograms} shows representative spectrograms for two diminished chord classes under training and evaluation conditions. Diminished chords display a compressed, more uniform energy distribution, and the harmonic stack remains geometrically regular along the frequency axis, confirming that CQT features embed harmonic structure in a form well-suited for convolutional pattern recognition.

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{Fig2.eps}
\caption{CQT spectrograms of two chord classes under training and evaluation conditions.
\textbf{(A)}~A\_diminished\_4 (training);
\textbf{(B)}~F\_diminished\_4 (training);
\textbf{(C)}~A\_diminished\_4 (evaluation);
\textbf{(D)}~F\_diminished\_4 (evaluation).}
\label{fig:cqt-spectrograms}
\end{figure}

\subsection{CNN training}\label{subsec4_2}

The CNN model exhibits rapid and stable convergence, where training accuracy increases from 47.65\% at epoch 1 to 98.41\% at epoch 2, reaching a peak of 99.91\% at epoch 8, while validation accuracy reaches 100\% by epoch 3. EarlyStopping is triggered after epoch 10, saving the best weights from epoch 4. The characteristic gap between training accuracy (constrained by dropout stochasticity) and validation accuracy (evaluated with dropout disabled) during early epochs is consistent with a well-regularized model with no overfitting. Evaluation on the reserved test set yields perfect accuracy, precision, recall, and F1-score of 1.0000 across all 36 classes. Training curves and the in-domain test confusion matrix are provided in Online Resources~3 and~4.

Under stratified 5-fold cross-validation on the full training set, the CNN attains perfect validation and test accuracy (1.0000) on every fold, with zero cross-fold variance, confirming that the result is not an artifact of a particular data split. Mean training accuracy remains slightly below 1.0000 (0.9976, std.\ 0.0007) solely due to dropout stochasticity during training; folds terminate between 11 and 21 epochs under EarlyStopping. Because all folds are identically perfect, fold-level metrics are omitted from the main text; full per-fold accuracy and loss tables and cross-validation training curves are provided in Online Resources~5--7.

\subsection{SVM hyperparameter tuning}\label{subsec4_3}

Optuna converges at trial 7 with optimal parameters $C = 1000$ and $\gamma = 10^{-5}$, at which the SVM achieves perfect accuracy on the reserved test set. The high value of $C$ indicates a near-hard-margin solution consistent with the high in-domain CQT feature discriminability. Hyperparameter search trajectories are shown in Online Resource~8. Under the same stratified 5-fold protocol as the CNN, the RBF-SVM likewise attains perfect accuracy and macro/weighted F1-score of 1.0000 on every fold with zero variance (Online Resources~9 and~10). Because both classifiers are identically perfect in-domain, fold-level SVM metrics are omitted from the main text. This equivalence is a key control result: the SVM's perfect in-domain performance rules out any explanation for subsequent cross-domain failure based on insufficient model capacity or suboptimal hyperparameters. The failure, when it occurs, is structural rather than configural.

\subsection{Cross-domain evaluation}\label{subsec4_4}

The trained CNN and optimized SVM are each evaluated on the 1,440-sample evaluation dataset without fine-tuning or domain adaptation. Table~\ref{tab:full-comparative-results} presents the full comparative results across both evaluation conditions.

\begin{table*}[h]
\caption{CNN vs.\ SVM full comparative results across in-domain and cross-domain evaluation}\label{tab:full-comparative-results}
\begin{tabular*}{\textwidth}{@{\extracolsep\fill}lllllll}
\toprule
 & \multicolumn{3}{l}{CNN} & \multicolumn{3}{l}{SVM} \\
\cmidrule(lr){2-4} \cmidrule(lr){5-7}
Metric & Validation & Testing & Evaluation & Validation & Testing & Evaluation \\
\midrule
Accuracy  & 1.00 & 1.00 & 0.89 & 1.00 & 1.00 & 0.29 \\
Precision & 1.00 & 1.00 & 0.91 & 1.00 & 1.00 & 0.64 \\
Recall    & 1.00 & 1.00 & 0.89 & 1.00 & 1.00 & 0.29 \\
F1-score  & 1.00 & 1.00 & 0.87 & 1.00 & 1.00 & 0.36 \\
\bottomrule
\end{tabular*}
\end{table*}

\subsubsection{CNN cross-domain performance}\label{subsubsec4_4_1}

The CNN retains substantial predictive capability under domain shift. The resulting confusion matrix (Fig.~\ref{fig:cnn-eval-confusion}) shows that the majority of chord classes are classified with perfect or near-perfect accuracy, including all B, C$\sharp$, D$\sharp$, D, F$\sharp$, F, G, and G$\sharp$ major and minor classes. Misclassifications are not randomly distributed but are concentrated in harmonically proximate chord pairs. The largest single error (A\_diminished $\to$ A$\sharp$\_major, 20 counts) involves a quality shift combined with a semitone root displacement, consistent with a global downward frequency shift of the evaluation instrument's spectral envelope moving the CQT pattern of A\_diminished into the neighborhood of A$\sharp$\_major. The next largest cluster (C\_diminished $\to$ C$\sharp$\_diminished, 16 counts) is a pure semitone root shift with preserved quality. All remaining errors involve either neighboring semitone roots or adjacent qualities (major to minor or minor to diminished), never large-interval confusions across unrelated classes.

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{Fig3.eps}
\caption{CNN confusion matrix on the cross-domain evaluation dataset
(1,440 samples, 40 per class). Diagonal dominance is maintained
across most classes; misclassifications are concentrated in a small
number of harmonically related chord pairs.}
\label{fig:cnn-eval-confusion}
\end{figure}

This error pattern is consistent with the CNN having learned CQT interval patterns that are partially robust to global spectral shifts, but failing at boundary cases where the shift displaces a test pattern across a class boundary defined by a single semitone. The overall accuracy of 0.89 and macro F1-score of 0.87 on 1,440 cross-domain samples demonstrate meaningful generalization beyond the training domain. A complete per-class misclassification list is provided in Online Resource~11.

\subsubsection{SVM cross-domain performance}\label{subsubsec4_4_2}

The SVM exhibits severe generalization failure. The resulting confusion matrix (Fig.~\ref{fig:svm-eval-confusion}) reveals that the model overwhelmingly predicts C\_diminished\_4 for the large majority of test samples regardless of the true label. This single-class attractor failure arises because the RBF kernel measures Euclidean distance between a test sample's 40,608-dimensional flattened CQT vector and the training support vectors.

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{Fig4.eps}
\caption{SVM confusion matrix on the cross-domain evaluation dataset
(1,440 samples). The near-complete concentration of predictions on
a single class (C\_diminished\_4) constitutes a model collapse under
acoustic domain shift.}
\label{fig:svm-eval-confusion}
\end{figure}

When the Yamaha PSR-S910's different piano voice synthesis produces CQT vectors with a globally shifted spectral envelope due to different harmonics, the distances to all support vectors are systematically altered. The support vectors of C\_diminished\_4 happen to occupy the position closest to the new distribution's center of mass, producing the observed attractor. The SVM achieves cross-domain accuracy of 0.29 and macro F1-score of 0.36.

The precision-recall asymmetry of 0.64 vs.\ 0.29 characterizes this attractor behavior: precision for C\_diminished\_4 remains relatively high since true samples of that class are correctly identified, while recall collapses for all other 35 classes. This failure is consistent with the theoretical prediction for RBF-SVM under covariate shift: when the fixed kernel bandwidth $\gamma = 10^{-5}$ is calibrated to the training distribution and the feature distribution shifts by a global spectral offset, the Gaussian kernel $k(\mathbf{x}, \mathbf{x}_i) = \exp(-\gamma \|\mathbf{x} - \mathbf{x}_i\|^2)$ approaches zero for all support vectors except those closest to the shifted distribution's center of mass.

\subsubsection{Comparative analysis}\label{subsubsec4_4_3}

The decisive performance divergence presented in Table~\ref{tab:full-comparative-results} provides a clear answer to which classifier architecture is structurally superior for out-of-domain audio deployments: the CNN vastly outperforms the RBF-SVM under acoustic covariate shift, retaining a strong macro F1-score of 0.87 compared to the SVM's total collapse to 0.36. The primary theoretical novelty of this finding lies in demonstrating that in-domain perfection (where both models achieved identical, flawless 1.00 metrics across 5-fold cross-validation) is entirely uninformative of a model's safety margins under domain shift[cite: 1].

When contextualized against the broader literature, these results expand upon the findings of Li \cite{Li2021} and Wolf-Monheim \cite{Wolf-Monheim2025}. While Li established that CQT features provide superior baseline spatial alignment for neural networks relative to STFTs \cite{Li2021}, our work proves that the utility of this feature space is entirely dependent on the spatial locality of the subsequent classifier. The RBF-SVM collapses because it flattens the CQT matrix and calculates global Euclidean distances, treating localized harmonic patterns and unmodeled room acoustics with identical weight—a property that makes global kernel methods inherently brittle across varying recording conditions. Conversely, the CNN's shared convolutional weights exploit the translational consistency of the CQT scale, allowing local pitch intervals to be recognized even when the global spectral envelope shifts. This establishes a clear architectural directive for the audio domain: local weight-sharing mechanisms are fundamentally superior to global distance-margin methods when cross-dataset deployment is expected.

\section{Conclusion}\label{sec5}

This paper presented a systematic evaluation of CQT spectrogram feature robustness and classifier generalization under acoustic domain shift using 36-class piano chord classification as a controlled experimental platform. Under in-domain 5-fold cross-validation both CNN and SVM achieve perfect and indistinguishable accuracy and F1-score of 1.00, confirming that the CQT feature space is fully discriminative under consistent recording conditions.

Under cross-domain evaluation, CNN achieves 0.89 accuracy and 0.87 macro F1-score while RBF-SVM collapses to 0.29 accuracy and 0.36 macro F1-score through a single-class attractor failure. This divergence reflects the structural difference between CNN's spatially local hierarchical feature learning, which partially preserves relative spectral interval patterns across the domain shift, and RBF-SVM's global kernel boundary, which is rigidly calibrated to the training feature distribution and cannot accommodate shifts in the global spectral envelope.

Crucially, this study demonstrates that in-domain validation metrics create a false sense of security, reinforcing the necessity of cross-dataset evaluation as a standard benchmark for audio classifiers. Our direct comparison concludes that the CNN is structurally superior for handling unmodeled environmental changes due to its localized receptive field. Furthermore, because the CNN's remaining errors are not random failures but are strictly concentrated in harmonically proximate, semitone-shifted chord pairs, this research provides highly targetable guidance for future engineering: practitioners should bypass generic noise injections and instead focus entirely on domain-specific augmentations, such as random spectral tilting and localized frequency-bin masking, to explicitly safeguard the CNN's decision boundaries against inter-instrument timbral shifts.

Future work includes extension of the chord vocabulary beyond fourth octave triads and more dataset with shifts regarding instrument and recording environment. More broadly, the results support the claim that spatially local hierarchically learned representations are more robust to acoustic covariate shift than global kernel methods in the spectrogram domain, a conclusion relevant to any spectrogram-based classification system deployed under variable recording conditions.

\backmatter

\bmhead{Supplementary information}
The online version constains supplementary materials (Online Resources~1--12) available at.

\bmhead{Author contributions}
All authors contributed to study conceptualization and design. Material preparation, data acquisition, experimentation, analysis, and drafting were performed by CGS. All authors reviewed and approved the final version.

\bmhead{Funding}
No funding was received for this work.

\bmhead{Data availability}
The piano chord dataset is publicly available at \url{https://zenodo.org/records/21077144}.

\bmhead{Code availability}
The dataset generation and recording tool is available at \url{https://github.com/sglkc/chords-dataset-generator/}.

\section*{Declarations}

\bmhead{Conflict of interest}
The authors declare no competing interests.

\bmhead{Ethics approval}
Not applicable.

\bmhead{Consent for publication}
All authors approved the final manuscript.

\bibliography{mendeley}

\end{document}
